export const metadata = {
  title: 'Video Editing',
  description: 'Video Editing Services To Make Your Videos Stand Out',
};

export default function VideoEditingLayout({ children }) {
  return children;
} 